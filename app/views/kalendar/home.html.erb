<%= month_calendar(attribute: :start_at, end_attribute: :end_at, events: @tasks) do |date, tasks| %>
  <%= date.day %>

  <% if tasks.present? %>
    <button>
      <p><%= tasks.count %>件予定あり</p>
    </button>
  <% end %>
<% end %>
